export class Element {
    name: string;
    weight: number;
    symbol: string;

    constructor (    name: string, weight: number, symbol: string) {
this.name = name;
this.weight = weight;
this.symbol = symbol;
    }
  }
